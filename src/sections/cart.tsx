import { Heading, VStack } from "@chakra-ui/react";

const Cart = () => {
  return (
    <VStack w="full" h="full" p={10} bg="gray.50">
      <Heading size="l">⬇️ Build this side with Chakra components ⬇️</Heading>
    </VStack>
  );
};

export default Cart;
